import { SetupComponent } from "../CommonSetupComponent"

const Divisions = () => {
  const data = {
    title: 'Divisions',
    url: 'Divisions',
  }
  return (
    <div>
      < SetupComponent data={data} />
    </div>
  )
}

export { Divisions }

