import { SetupComponent } from '../CommonSetupComponent'

const Categories = () => {
  const data = {
    title: 'Categories',
    url: 'Categories',
    }
  return (
    <div>
      < SetupComponent data={data} />    
    </div>
  )
}

export { Categories }

